FROM node:22-alpine

WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY src ./src
COPY jsconfig.json ./jsconfig.json

ENV NODE_ENV=production
EXPOSE 4001

CMD ["npm", "run", "analytics:serve"]
